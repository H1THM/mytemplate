import './styles.css';


//new repo setup commands:
//git clone repo-url repo-new-name
//npm install   (install dependinces found in package.json)
//npm start    (webpacklive server)
//npn run build (creates dist/ and bundles everything, last step)